name: client_hisab_app
description: Client balance, payment, work, PDF, WhatsApp and backup app.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.3
  file_picker: ^10.3.2
  path_provider: ^2.1.5
  share_plus: ^11.1.0
  url_launcher: ^6.3.2
  pdf: ^3.11.3
  printing: ^5.14.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
